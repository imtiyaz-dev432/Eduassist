server:
  http_listen_port: 3200

target: all

storage:
  trace:
    backend: local
    local:
      path: /tmp/tempo/blocks

distributor:
  receivers:
    otlp:
      protocols:
        grpc:
          endpoint: 0.0.0.0:4317
        http:
          endpoint: 0.0.0.0:4318